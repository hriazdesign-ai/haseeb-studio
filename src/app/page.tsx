import { BlocksHomePage } from "@/components/home-parallax/BlocksHomePage";

/**
 * Live homepage — approved blocks prototype (formerly `/home-parallax-blocks`).
 * Previous homepage markup/components remain in the repo for comparison
 * (e.g. `WorkSection`, `homeProjects`) until fully retired.
 */
export default function Home() {
  return <BlocksHomePage />;
}
