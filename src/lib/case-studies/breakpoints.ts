/**
 * Shared art-direction breakpoint for `mobileSrc`.
 * Below this width the mobile asset is used; tablet and desktop keep `src`.
 * Matches Tailwind `md` (768px).
 */
export const ART_DIRECTION_MOBILE_MAX_PX = 767;

/** @deprecated Prefer `ART_DIRECTION_MOBILE_MAX_PX`. */
export const LARGE_FEATURE_MOBILE_MAX_PX = ART_DIRECTION_MOBILE_MAX_PX;
